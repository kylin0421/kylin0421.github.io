source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

gem "tzinfo-data"
gem "faraday-retry"
# wdm only accelerates file watching for `jekyll serve`. Its last release does
# not build on the Ruby version used on this Windows machine, and it is not
# required for static builds or GitHub Pages deployment.

# If you have any plugins, put them here!
group :jekyll_plugins do
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
  gem "jekyll-gist"
  gem "jekyll-feed"
  gem "jemoji"
  gem "jekyll-include-cache"
  gem "jekyll-algolia"
end
